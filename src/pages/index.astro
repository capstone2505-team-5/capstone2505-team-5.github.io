---
import BaseLayout from '../layouts/BaseLayout.astro';
import NavBar from '../components/NavBar.astro';
import Hero from '../components/Hero.astro';
import Team from '../components/Team.astro';
import Image from '../components/Image.astro';
---

<BaseLayout>
	<NavBar currentPage="home" />

	<main class="main mt-20">
		<Hero />

		<section class="section alternating-section">
			<div class="container">
				<div class="section-content reverse">
					<div class="section-text">
						<h2>Guided Error Analysis Workflow</h2>
						<p>Follow a proven 5-step process to systematically improve your LLM application. From selecting traces to categorizing failures, LLMonade guides you through each stage of manual error analysis with clear visual progress tracking and expert-recommended best practices.</p>
						<a href="/case-study#our-solution" class="btn btn-secondary">See Workflow</a>
					</div>
					<div class="section-image">
						<Image 
							src="/images/LandingPage/workflow_landing_sym.mp4" 
							alt="Error Analysis Workflow"
							bordered={true}
							rounded={true}
							autoplay={true}
							loop={true}
							muted={true}
						/>
					</div>
				</div>
			</div>
		</section>

		<!-- Section 1: Image Right, Text Left -->
		<section class="section alternating-section">
			<div class="container">
				<div class="section-content">
					<div class="section-text">
						<h2>Frictionless Annotation Interface</h2>
						<p>Go from raw trace data to actionable insights in five simple steps. LLMonade walks you through the most impactful AI evaluation, manual error analysis. This powerful workflow surfaces the highest-ROI issues, so your development team can make targeted improvements with confidence.</p>
						<a href="/case-study/#our-solution" class="btn btn-secondary">Learn More</a>
					</div>
					<div class="section-image">
						<Image 
							src="/images/LandingPage/AnnotateHighQualityFastWeb1.mp4" 
							alt="Manual Review Demo"
							bordered={true}
							rounded={true}
							autoplay={true}
							loop={true}
							muted={true}
						/>
					</div>
				</div>
			</div>
		</section>

		<!-- Section 2: Image Left, Text Right -->
		<section class="section alternating-section">
			<div class="container">
				<div class="section-content reverse">
					<div class="section-text">
						<h2>Native Phoenix Integration</h2>
						<p>Deploy with Phoenix integration out of the box.  LLMonade automatically synchronizes your entire trace history and stays updated with new data as it arrives. Focus on what matters: analyzing your application's behavior and shipping improvements, faster.</p>
						<a href="/case-study#architecture" class="btn btn-secondary">See Workflow</a>
					</div>
					<div class="section-image">
						<Image 
							src="/images/LandingPage/landing_etl_pipeline_cropped.png" 
							alt="Phoenix Integration" 
							width="100%"
							height="400px"
							bordered={false}
							rounded={false}
							noShadow={true}
						/>
					</div>
				</div>
			</div>
		</section>

		<!-- Section 3: Image Right, Text Left -->
		<section class="section alternating-section">
			<div class="container">
				<div class="section-content">
					<div class="section-text">
						<h2>Automated Deployment to AWS</h2>
						<p>Deploy LLMonade in your AWS organization with ease. Our CLI tool leverages AWS CDK to automatically provision the entire serverless architecture for you. Set up the complete, self-hosted platform with a single command.</p>
						<a href="/case-study#architecture" class="btn btn-secondary">View Architecture</a>
					</div>
					<div class="section-image">
						<Image 
							src="/images/LandingPage/cli-combined-bordered-transparent.png" 
							alt="LLMonade CLI" 
							width="100%"
							height="100%"
							bordered={false}
							rounded={true}
							noShadow={true}
						/>
					</div>
				</div>
			</div>
		</section>

		<Team />
	</main>
</BaseLayout>

<style>
	.alternating-section {
		padding: 4rem 0;
	}

	.alternating-section:nth-child(odd) {
		background-color: var(--gray-50);
	}

	.section-content {
		display: grid;
		grid-template-columns: 1fr 1fr; /* Equal split - 50/50 */
		gap: 4rem;
		align-items: center;
	}

	.section-content.reverse {
		direction: rtl;
		grid-template-columns: 1fr 1fr; /* Equal split - 50/50 */
	}

	.section-content.reverse > * {
		direction: ltr;
	}

	.section-text h2 {
		font-size: 2.5rem;
		font-weight: 700;
		color: var(--primary-blue);
		margin-bottom: 1.5rem;
		line-height: 1.2;
	}

	.section-text p {
		font-size: 1.1rem;
		line-height: 1.7;
		color: var(--gray-700);
		margin-bottom: 1.5rem;
	}

	.section-text .btn {
		margin-top: 1rem;
	}

	.section-image {
		display: flex;
		justify-content: center;
		align-items: center;
	}

	@media (max-width: 768px) {
		.section-content {
			grid-template-columns: 1fr;
			gap: 0.5rem; /* Reduced from 1rem to minimize text-to-image gap */
		}

		.section-content.reverse {
			direction: ltr;
		}

		.section-text {
			margin-bottom: 0.5rem; /* Add small margin after text */
		}

		.section-text h2 {
			font-size: 2rem;
			margin-bottom: 1rem; /* Reduce space after heading */
		}

		.section-text p {
			font-size: 1rem;
			margin-bottom: 1rem; /* Reduce space after paragraph */
		}

		.section-text .btn {
			margin-bottom: 0.5rem; /* Reduce space after button */
		}

		.alternating-section {
			padding: 1rem 0;
		}

		.section-image {
			margin: 0;
			padding: 0;
		}

		/* Specific targeting for the CLI section to reduce gaps */
		.alternating-section:last-of-type {
			padding-top: 0.5rem;
			padding-bottom: 0.5rem;
		}

		.alternating-section:last-of-type .section-content {
			gap: 0.25rem; /* Even smaller gap for CLI section specifically */
		}

		/* Reduce space between alternating sections */
		.alternating-section + .alternating-section {
			margin-top: -0.5rem;
		}
	}
</style>

